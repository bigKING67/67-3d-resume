---
schema: resume-design-authority-v1
surface: scroll-driven-3d-resume
platform: web
theme: single-dark-cinematic
status: enforced
last_verified: 2026-09-03
---

# DESIGN.md

## Design read

这是一个面向招聘者、潜在合作者和同行的滚动式 3D 个人简历：气质应当有触感、电影感和一点玩心，但第一任务仍是让访问者快速看懂“六七是谁、做过什么、正在做什么”。3D 是叙事媒介，不是遮挡履历的特效层。

## 核心原则

1. **故事优先于炫技。** 每个镜头、贴纸和动效都要强化身份或经历；不能解释职业叙事的装饰应删除。
2. **信息始终可读。** 3D 背景可以复杂，文字层必须通过暗色 scrim、磨砂轨道、留白和稳定排版获得对比度。
3. **一品牌一枚标识。** 同一外部公司或工具只保留一个可见 Logo；重复不是节奏，而是噪声。
4. **Logo 是标识，不是卡片。** 公司和工具使用透明轮廓，不包无意义的白色矩形；个人标签才可以使用白色模切边。
5. **保留手工感。** 胶片噪点、轻微景深、非完全规整的贴纸角度和人物表面共同形成触感，但不能制造脏边、锯齿、白晕或随机堆叠。
6. **克制新增视觉语言。** 先复用当前字体、颜色、玻璃材质、发丝线和运动曲线；没有明确产品理由，不引入第二套卡片、渐变、圆角或图标风格。

## 颜色

| 角色 | 当前权威值或表现 |
| --- | --- |
| 页面底色 | `#0a0e16` 深炭蓝黑 |
| 主文字 | `#f4f1ea` 暖白 |
| 次文字 | `rgba(244, 241, 234, 0.6)` |
| 发丝分隔 | `rgba(244, 241, 234, 0.14)` |
| 强调色 | `#ffd9b3` 暖杏色 |
| 场景渐变 | `#6f906f` 到 `#dbd3b5` 的低饱和绿/米色 |

当前只有一套暗色电影主题，不制造未实现的亮色主题承诺。新增语义颜色或字体角色时，先扩展这里，再落到代码；不要在单个组件里随意创造视觉常量。

## 字体

| 角色 | 当前权威值或表现 |
| --- | --- |
| 正文/UI | Helvetica Neue、Chiron GoRound TC、苹方等系统回退 |
| 编辑感标题 | Cormorant Upright |
| 手写签名 | Mansalva |

正文先保证中英文可读和稳定字宽，编辑感字体只用于少量标题，手写字体只用于签名式强调；不得把装饰字体扩散到长段履历。

## 组件与表面

- 固定 3D Canvas 是持续叙事底层，不按普通内容卡片切割。
- 履历轨道使用右侧暗化、轻磨砂与发丝分隔来建立阅读面，不增加厚重悬浮卡。
- 外部品牌 Logo 与个人贴纸是两类组件：前者直出透明轮廓，后者可以使用白色模切边。
- 作品详情沿用深色实面、暖白文字、发丝边和同一强调色，不另起一套视觉系统。

## 构图与响应式

- 桌面端让人物和贴纸承担左侧/中央视觉叙事，履历时间轴保持右侧稳定阅读区。
- 首屏、履历和作品区是一条连续镜头，不应像三个互不相关的网站区块。
- 移动端优先文字可读和关键经历，允许履历使用半透明深色面板；不得通过横向溢出保留桌面构图。
- Logo 允许随模型透视、表面和景深变化，但不能与履历文字竞争，也不能依靠额外白色卡片获得存在感；品牌官方 app icon 自带的锁定底形不属于额外卡片。
- 作品详情可以更实、更深，但仍使用同一暖白、炭蓝灰、发丝边和强调色体系。

## 作品媒体

- 作品页的暗色环境是展示台，正文媒体可以使用 DataHub 已建立的暖白/浅灰“证据板”作为内容表面；它是同一视觉系统里的项目产物，不是第二套亮色页面主题。
- 栏目封面必须在卡片尺寸下表达一组作品的共同能力；详情 Hero 必须表达具体项目的对象、动作或结果。两者不得复用同一张图，也不把完整架构图裁成无法理解的横条。
- 同一作品的价值、工程和证据媒体保持色彩、边角、噪点、注释与排版家族一致，但画面结构随内容变化；不使用一套节点图模板替换七个不同 Craft 的真实差异。
- AI Craft 使用深墨文字、暖白证据板、发丝分隔与每个项目一种克制信号色。复杂流程放正文，Hero 先让人不读小字也能看懂项目在处理什么。
- 精确中文、指标、命令和路径不得交给图像模型自由生成；真实截图只做必要裁切、脱敏和注释，不套无意义设备壳或伪造产品界面。
- 桌面与窄屏 Hero 分别构图；关键对象、文字和证据不得依赖 `object-fit: cover` 的偶然裁切。完整媒体职责与规格见 [`docs/WORKS-STORYTELLING.md`](docs/WORKS-STORYTELLING.md)。

## 贴纸与品牌

### 外部品牌

- 每个品牌最多一枚可见贴纸。
- Codex：按用户当前定义，使用暖白圆角底中的黑色 OpenAI 结形图标作为一枚可见工具贴纸；以“角色”履历节点的相机画面为构图基准，位于 `ANALYST` 下方的近侧脸颊，当前对象尺度为 `0.54`，第 100 帧中心约为 `(607, 707)`，不下坠出画面，也不额外增加第二枚 OpenAI / ChatGPT 结形标识。
- 火奴：旧绿色锁定版已按用户要求替换为官网声明的青蓝圆形 `HN` favicon；保留透明背景，不自行改色、重画或增加白色卡底，同一画面也不在履历文字区重复第二枚 Logo。
- 诗裴丝：保留一个绿色圆形 Logo。
- Claude：使用 `claude.ai/favicon.svg` 的官方橙色星芒，并采用用户参考中的深炭圆角 app-icon 锁定底；外部四角保持透明，不增加额外白卡。以“角色”履历节点的相机画面为构图基准，位于 `ANALYST` 上方的近侧脸颊，当前对象尺度为 `0.52`，与下方 Codex 形成上—中—下的错落节奏。
- ChatGPT：当前不作为独立可见贴纸；不得再用同一 OpenAI 结形图标复制一枚 ChatGPT，破坏“一品牌一枚标识”的约束。
- Groland：使用官网页面当前声明的 32×32 favicon 原始图形，保留其中的白色方形锁定底与黑色图形；不提取成透明线稿，不同时再放字标，也不额外叠第二层白卡。

### 个人贴纸

- `I ♥ HZNU`、`ANALYST`、`sixseven` 与六七个人 Agent Logo 属于个人身份和语气；前三者可以保留白色模切轮廓，个人 Agent Logo 保持用户提供的黑底白色 `67` 旋形 app-icon 形态与透明圆角外缘。
- `I ♥ HZNU` 使用粗重黑字、红心和随字形连接的暖白模切边，不再使用整块白色横条；允许有轻微错位和阴影，但必须保持一眼可读。
- `ANALYST` 采用上游 `HOTSAUR` 的长条连体模切结构与粗重深色字面，只保留文字，不保留前置手势；标题语义是职业角色，不是装饰口号。
- `sixseven` 采用上游 `ZOOOP` 的荧光黄字、粗黑内描边和起伏白色外模切结构，内容始终保持个人标识 `sixseven`。
- 旧 atlas 中的白色 `67` 旋涡字形仍不恢复：其单元曾因完全不透明黑底在运行画面中呈现为意外黑色方块，对应 `sticker13` 与 atlas 单元保持删除。用户新提供的高分辨率个人 Agent Logo 是独立资产，复用左脸原 `BUILDER` 面片 `sticker10`，不得误占或替换右脸 `sticker1 / ANALYST`。
- `BUILDER` 不再作为可见贴纸；其左脸面片 `sticker10` 只承载一枚六七个人 Agent Logo，用来同时表达本人及本人的 Agent，保持原有斜向角度、曲面顶点与对象变换。
- 足球/棋盘球用于表达本人喜欢踢足球，可以恢复为一枚可见贴纸；不得与 Claude 或其他品牌贴纸占用同一对象位置。
- 个人贴纸也要有信息差异；不要为了填满人物表面复制同一句话或同一个符号。

### 质量门槛

- 透明像素边缘不得出现白色矩形、白晕、横向残片或压缩脏边。
- 缩小、倾斜和景深状态下仍能识别品牌轮廓。
- Claude、`ANALYST` 与 Codex 在“角色”履历节点形成上—中—下的同一视觉组：Claude 位于上方留白，`ANALYST` 作为中部主轴，Codex 位于下方留白；两枚工具贴纸必须与 `ANALYST` 保持清晰间隔，不聚成下方按钮行。
- 左脸公司贴纸与右侧履历节点实行一对一视觉接棒：`focus-3 → HN / 火奴`、`focus-4 → SPES`、`focus-5 → Groland`。当前公司贴纸必须落在景深主焦点，其他公司贴纸保留为职业路径上下文但不得拥有同等视觉权重；贴纸不因节点切换而瞬移或交换物理位置。
- 火奴、SPES、Groland 三段属于同一个“左脸公司贴纸舞台”：允许在贴纸之间移动焦点，但 Groland 的主要阅读状态不得提前切回完整正脸。Groland 离场与 Works 入场的交界段才从侧脸回到正脸，同时把焦点从 Groland 平滑移到脸部中心、放宽景深，并让三枚公司 Logo 恢复正常层级；画廊钉住时必须形成一拍完整清晰正脸，随后 Works 横移接管才转向后脑勺。
- `focus-2` 继续对应“角色”履历节点，以 Claude—`ANALYST`—Codex 组合为主视觉；这一组与左脸公司经历接棒是两套语义，不得互相替换。
- Claude 与 Codex 的视觉权重应接近上游小贴纸相对主文字贴纸的层级，不得缩成状态图标；两枚贴图的可见 Alpha 边界保持接近 `1:1`，对象只允许三轴等比例缩放，不为某个相机帧强行拉宽或压扁。当前 Claude / Codex 对象尺度分别为 `0.52` / `0.54`；Codex 因暖白底留白更大而使用略高几何尺度，以最终屏幕光学体量接近为准，不能反过来与 `ANALYST` 争夺主视觉。
- Claude 与 Codex 必须在“角色”履历节点及其阻尼相机仍显示该条文字的过渡区间中完整可见，贴合近侧脸部曲面并避开嘴部、`ANALYST` 与履历文字；桌面验收至少覆盖 CameraAction 第 100–115 帧，不能只检查节点定格帧、对象存在或 UV 正确，也不得把面片留在头部远侧造成整枚遮挡，不得出现平面穿脸、Logo 镜像、透明发灰或边缘被裁切。
- Claude 橙色星芒不得直接落在近似橙色肤色上形成低对比消失；当前权威形态为深炭 app-icon 底上的橙色星芒，与下方暖白底 Codex 形成一深一浅的工具标识呼应。
- `sixseven` 的荧光黄字与黑描边应占据贴纸主体，暖白只承担模切边；不得因白边过宽或高光覆盖而在首屏失去可读性。
- 六七个人 Agent Logo 应位于 `HN` 下方的左脸原 `BUILDER` 位置，缩小和倾斜时仍能辨认白色 `67` 旋形；右脸 Claude—`ANALYST`—Codex 组合必须保持不变。
- 桌面 Hero 的视觉基准采用用户选定的上游首屏：保持上游 GLB 相机、模型 `2.25` 缩放和 `[0, 0.4, -0.7]` 位移，不另加桌面端推近/拉远补偿；以同视口、`scrollY=0` 的人物包围框做回归，避免浏览器恢复滚动或陈旧运行态造成误判。
- 修改 GLB 后至少复核两个桌面滚动节点和一个窄屏节点。

## 运动

- 滚动驱动相机是主运动；其他动画应服务于定位、进入、反馈或层级，避免与镜头争夺注意力。
- 优先使用 transform 和 opacity；持续循环只用于低注意力提示。
- 当前仅部分组件实现 `prefers-reduced-motion`。在宣称无障碍运动完整前，必须补齐并真实验证全页降级；文档不能替代实现。
- 相机、景深、Bloom 和噪点的调整必须在真实浏览器观察，不从常量或构建成功推断“更顺滑”。`CameraAction` 第 250 帧仍是 Groland 的公司侧脸；只有履历结束后才在第 250–300 帧回到完整正脸，并同步完成焦点回中和景深放宽；人物自身则只在 Works 接管后的第 300–350 帧转向后脑勺。不得出现“右侧仍是 Groland、左侧已回正脸”“履历已结束、正脸仍整体失焦”或“相机尚未回正、人物已经转走”的中间状态。

## 文案与交互

- 文案直接、第一人称、有职业判断，不使用空泛的“赋能、引领、极致”等包装词。
- 外链和作品入口要说明去向；键盘焦点必须可见。
- 加载态要让用户知道 3D 场景正在准备；失败不能静默成黑屏。
- 页面应提供明确的博客返回路径，但不能用常驻重型导航破坏沉浸式首屏。

## Do / Don’t

**Do**

- 用镜头和对焦连接履历节点。
- 用少量有意义的贴纸呈现经历和工具谱系。
- 保持暖白、低饱和背景、发丝线和编辑感排版。
- 让移动端成为可读的重新构图，而不是桌面缩小版。

**Don’t**

- 不重复同一品牌 Logo。
- 不给公司 Logo 套白色方卡。
- 不新增与现有语言无关的发光玻璃卡、彩色渐变或大圆角组件。
- 不因上游更新覆盖个人模型、文案、作品和本设计合同。
- 不把截图、静态检查或构建通过单独当作视觉验收。

## 权威与演进

- 当前运行页面和用户明确反馈高于本文；确认新的长期视觉决策后同步更新本文。
- 上游 [`sen-3d-resume`](https://github.com/dayinji/sen-3d-resume) 是结构与交互底座，不是本项目的最终视觉权威。
- 视觉变更完成前执行同类组件一致性检查：比较所有品牌贴纸、个人贴纸、桌面/移动端和适用滚动节点，最终状态为 `pass`、`blocked` 或 `incomplete`。
